// build.gradle.kts (Project: HomeBookkeeping)
plugins {
    // Убедись, что эти плагины и их версии присутствуют.
    // Версии могут отличаться, но убедись, что они актуальны.
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false // Важно для Kotlin
    id("com.google.gms.google-services") version "4.4.1" apply false
}
